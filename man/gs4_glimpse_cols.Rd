% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glimpse-cols.R
\name{gs4_glimpse_cols}
\alias{gs4_glimpse_cols}
\title{Glimpse a data frame's columns alongside their spreadsheet column letters}
\usage{
gs4_glimpse_cols(data)
}
\arguments{
\item{data}{A data frame.}
}
\value{
A data frame with one row per column of \code{data} and columns
\code{letter}/\code{name}, returned invisibly. Also printed as a compact,
aligned preview -- wrapped across multiple blocks if every column
doesn't fit on one line, the way \code{\link[utils:str]{utils::str()}} (or dplyr's
\code{glimpse()}) wrap wide data.
}
\description{
A quick reference for which spreadsheet column letter each of \code{data}'s
columns will land in when written with \code{googlesheets4::range_write()}/
\code{sheet_write()}/\code{\link[=write_pretty_sheet]{write_pretty_sheet()}} -- handy right before writing an
A1-style \code{range} by hand instead of using \code{\link[=gs4_cols]{gs4_cols()}} (which sidesteps
this entirely by matching on column name rather than letter).
}
\examples{
gs4_glimpse_cols(mtcars)
}
